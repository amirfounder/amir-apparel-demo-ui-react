import { getProductById } from "./ProductPageService";

describe('get product by id', () => {

  test('add not-existing product', () => {
    const
      mockProductId = 1,
      setProduct = jest.fn(),
      setApiError = jest.fn();
    
  })

  test('add existing product', () => {
  })

})